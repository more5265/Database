CREATE TABLE `member` (
  `uid` varchar(50) NOT NULL,
  `name` varchar(50) NOT NULL,
	`hp` varchar(20) NOT NULL,
	`pos` VARCHAR(20) NOT NULL,
	`dep` VARCHAR(20) NOT NULL,
  PRIMARY KEY (`uid`)
  
user6CREATE TABLE `User6` LIKE `User3`;

  
  
  
  
  
  
  
);



